# Copy this file to .env and fill in your values
# The frontend talks to /api by default, so VITE_API_BASE_URL is optional
# unless you plan to host the API on a separate origin.

VITE_API_BASE_URL=
API_PORT=4100
PORT=4100
AUTH_SESSION_DAYS=21
HOPIN_DB_PATH=./data/hopin.sqlite
